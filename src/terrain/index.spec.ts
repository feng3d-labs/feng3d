import { assert, describe, it } from 'vitest';

import { describe } from 'vitest';

describe('test', () =>
{
    it('test', () =>
    {
        // terrain module test
    });
});

describe('test', () =>
{
    it('test', () =>
    {
        assert.ok(!!terrain);
    });
});
