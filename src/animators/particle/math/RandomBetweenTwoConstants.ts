namespace feng3d
{
    /**
     * 两个常量间取随机值
     */
    export class RandomBetweenTwoConstants implements IMinMaxCurve
    {
        /**
         * 获取值
         * @param time 时间
         */
        getValue(time: number)
        {
            // return this.value;
            return 0;
        }
    }
}