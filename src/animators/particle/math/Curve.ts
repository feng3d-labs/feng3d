namespace feng3d
{
    /**
     * 曲线
     */
    export class Curve implements IMinMaxCurve
    {
        /**
         * 获取值
         * @param time 时间
         */
        getValue(time: number)
        {
            // return this.value;
            return 0;
        }
    }
}