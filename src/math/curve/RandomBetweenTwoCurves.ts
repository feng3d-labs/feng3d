namespace feng3d
{
    export class RandomBetweenTwoCurves implements IMinMaxCurve
    {
        /**
         * 获取值
         * @param time 时间
         */
        getValue(time: number)
        {
            // return this.value;
            return 0;
        }
    }
}