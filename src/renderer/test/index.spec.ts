import { assert, describe, it } from 'vitest';

import * as renderer from '../src';

describe('test', () =>
{
    it('test', () =>
    {
        assert.ok(!!renderer);
        assert.ok(true);
    });
});
