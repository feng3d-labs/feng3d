# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
dist/
lib/
public/
*.tsbuildinfo

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/
test-results/

# Environment
.env
.env.local
.env.*.local

# Temporary
*.tmp
.temp/
.cache/

# Playwright MCP
.playwright-mcp/

# Playwright 视觉回归测试
# 基线截图 .verify/<spec>.ts/*.png 入库（作为对比基准）；
# 仅忽略运行期产生的临时输出与报告
.verify/output/
.verify/report/
# 编辑器 e2e 专用配置（playwright.editor.config.ts）的输出，同样只是运行期产物
.verify/editor-output/
.verify/editor-report/
# .verify 根目录下的临时诊断脚本（非基线文件，如 API 连通性探针）
.verify/*.mjs
.verify/*.txt
.verify/*.log
.verify/*.tsv
.verify/*.patch
# .verify 根目录平铺的调试截图（一次性诊断产物）；
# 基线截图位于 .verify/<spec>.ts/ 子目录，不受本规则影响
.verify/*.png

# Debug screenshots (root level only)
/*.png
/screenshot*.png
tmp/
.zcode
lint_tmp.json

# 编辑器的用户覆盖层（issue #171）：本地、不入库。
# 模板见 packages/editor/editor.patch.example.json——要定制就复制成下面这两个位置之一：
#   · 开发时：packages/editor/editor.patch.json（dev server 根目录）
#   · 产物里：packages/editor/public/editor.patch.json（与 index.html 同级）
packages/editor/editor.patch.json
packages/editor/public/editor.patch.json
